{
  "name": "mythos-director",
  "private": true,
  "workspaces": ["apps/*", "packages/*", "agents/*"],
  "scripts": {
    "build": "npm run build --workspaces",
    "dev": "npm run dev --workspaces --if-present"
  }
}
